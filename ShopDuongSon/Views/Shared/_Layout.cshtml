<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title</title>
    @Styles.Render("~/Content/css")
</head>
<body>
    <div class="container">
        <header class="">
            @*<h1 class="nn-company">Java Technology Suite</h1>
            <h5 class="nn-slogan">The center point of the professional programming</h5>
            <img class="nn-logo" src="~/Content/images/header-object.png" />*@
            @Html.Partial("_Hearder")
        </header>

        <div class="navbar navbar-inverse">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
            </div>
            <div class="navbar-collapse collapse">
                @Html.Partial("_Menu")
            </div>
        </div>

        <div class="nn-sheet">
            <div class="row">
                <aside class="col-md-3">
                    @Html.Partial("_CartInfo")
                    @Html.Partial("_Search")
                    @Html.Action("Category", "Home")
                    @Html.Action("Supplier", "Home")
                    @Html.Partial("_Special")
                </aside>
                <article class="col-md-9">
                    <div class="nn-body">@RenderBody()</div>
                </article>

            </div>
            <hr />
            <div class="row">
                <footer style="text-align:center">
                    <p>&copy; @DateTime.Now.ToString("MMMM 2014") Shop Dương Sơn</p>
                </footer>
            </div>
        </div>
    </div>

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    <link href="~/CartEffect/jquery-ui.css" rel="stylesheet" />
    <script src="~/CartEffect/jquery-ui.js"></script>
    @RenderSection("scripts", required: false)

    <script>
        $(function () {
            $("#search").autocomplete({
                source: "/Home/Search",
                minLength:2
            });
        })
    </script>
</body>
</html>